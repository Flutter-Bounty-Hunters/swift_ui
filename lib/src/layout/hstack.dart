import 'package:flutter/widgets.dart';

// TODO:
class HStack extends StatelessWidget {
  const HStack({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
