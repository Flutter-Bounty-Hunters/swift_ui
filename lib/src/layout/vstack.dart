import 'package:flutter/widgets.dart';

// TODO:
class VStack extends StatelessWidget {
  const VStack({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
